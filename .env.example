# ============================================================
# 企业微信网关 MCP Server · 环境变量模板
# 复制本文件为 .env 后填入你自己的值：cp .env.example .env
# .env 已被 .gitignore 忽略，不会被提交，请妥善保管。
# ============================================================

# 你的企微网关 admin API 基址（部署网关后获得，结尾不含斜杠）
# 示例：https://gateway.your-domain.com/api/v1/admin
WG_BASE_URL=https://your-wechat-gateway.example.com/api/v1/admin

# 网关侧分配的服务令牌（对应网关后端 .env 的 MCP_API_TOKEN），必填
# 仅在你自己部署的网关与本地 MCP 之间使用，请勿泄露或提交到仓库。
WG_API_TOKEN=replace-with-your-own-mcp-api-token
